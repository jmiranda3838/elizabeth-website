{
  "name": "elizabeth-armstrong-therapy",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run check:types && npm run check:docs && npm run check:links && npm run check:format && npm run build",
    "check:types": "astro check",
    "check:docs": "node scripts/check-docs.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:format": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.1.0",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "prettier": "^3.9.3",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.9.0"
  }
}
