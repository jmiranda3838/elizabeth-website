{
  "name": "elizabeth-feedback-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that turns site review-overlay comments into GitHub issues.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
